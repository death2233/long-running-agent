---
description: 查看当前项目进度。读取 feature_list.json 和 claude-progress.txt，输出完成比例、当前目标和最近日志。用法：/long-running-agent:status
---

读取当前项目进度并汇报。

## 必须按顺序执行

1. 读取 `feature_list.json`，统计总功能数和已完成数（passes: true）
2. 读取 `claude-progress.txt`，提取最近一次 session 记录
3. 运行 `git log --oneline -5` 查看最近提交

## 输出格式

```
📊 项目进度报告
════════════════════════════════

进度：N/M 功能完成（XX%）

✅ 已完成：
  - [功能描述]（priority=N）
  ...

⏳ 待完成：
  - [功能描述]（priority=N）
  ...

📋 上次 session：
  [claude-progress.txt 最后一段]

💡 建议下一步：
  运行 /long-running-agent:run 继续开发
```

若 `feature_list.json` 或 `claude-progress.txt` 不存在，提示用户先运行 `/long-running-agent:init`。
